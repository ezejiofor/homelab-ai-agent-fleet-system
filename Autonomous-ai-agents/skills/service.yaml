---
apiVersion: v1
kind: Service
metadata:
  name: skillshub
  namespace: skillshub
  labels:
    app.kubernetes.io/name: skillshub
    app.kubernetes.io/part-of: autonomous-ai-agents
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: skillshub
  ports:
    - name: api
      port: 8000
      targetPort: api
      protocol: TCP
