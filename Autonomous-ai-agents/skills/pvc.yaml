---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: skillshub-storage
  namespace: skillshub
  labels:
    app.kubernetes.io/name: skillshub
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 10Gi
