---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: nemoclaw
  namespace: nemoclaw
  labels:
    app.kubernetes.io/name: nemoclaw
    app.kubernetes.io/part-of: autonomous-ai-agents
  annotations:
    # Vault Agent sidecar — injects NVIDIA NIM API key + OpenShell credentials
    vault.hashicorp.com/role: "nemoclaw"

