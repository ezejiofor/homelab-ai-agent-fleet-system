---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: openclaw
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw
  annotations:
    # Vault Kubernetes auth — reads secrets from secret/homelab/ai/openclaw
    vault.hashicorp.com/role: "openclaw"

