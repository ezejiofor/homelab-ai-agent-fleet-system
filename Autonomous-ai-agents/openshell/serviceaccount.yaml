---
# Mirrors openshell/templates/serviceaccount.yaml.
# Used by the gateway StatefulSet to manage Sandbox CRs in agents-system.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: openshell
  labels:
    app.kubernetes.io/name: openshell