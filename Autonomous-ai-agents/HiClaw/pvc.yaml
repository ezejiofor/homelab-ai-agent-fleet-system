---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hiclaw-workspace
  namespace: hiclaw
  labels:
    app.kubernetes.io/name: hiclaw
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block
  resources:
    requests:
      storage: 10Gi
